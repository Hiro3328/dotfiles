#!/bin/fish
function dmenu-wrapper
  fuzzel -d -p "🔒" --placeholder=" Type to Filter" $argv
end

function rbw-parser
  rbw ls --fields name,user | awk -F "\t" '{print $1 " <" $2 ">"}'  
end

rbw-parser | dmenu-wrapper | sed 's/ <\(.*\)>/\n\1/' | xargs -d "\n" -r rbw get | wl-copy 
