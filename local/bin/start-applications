#!/bin/fish
function start-audio
  /usr/bin/pipewire &
  /usr/bin/pipewire-pulse &
  /usr/bin/wireplumber &
end

killall pipewire pipewire-pulse wireplumber 
start-audio
sleep 0.5 
pidof ironbar || ironbar & 
sleep 0.5
pidof equibop || equibop &
exit
